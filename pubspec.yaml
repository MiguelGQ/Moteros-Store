name: votacion
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+7

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_alert: ^0.4.0
  flutter_local_notifications:
  country_code_picker: ^2.0.2

  # Firebase
  firebase_auth: ^3.3.6
  cloud_firestore: ^3.1.7
  firebase_core: ^1.12.0
  firebase_storage: ^10.2.6
  firebase_messaging:
  firebase_admin:
  firebase_auth_platform_interface: ^6.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0


flutter:

  uses-material-design: true
  assets:
    - images/
